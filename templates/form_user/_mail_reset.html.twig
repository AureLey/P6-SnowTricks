<h1>Reset password E-mail</h1>
<p>Hello {{ user.username }},</p>
<p>Click on the link below to reset your password:</p>
<p><a href="{{ absolute_url(path('resetpassword', {token: user.token})) }}">
        {{ absolute_url(path('resetpassword', {token: user.token})) }}
    </a>
</p>