{% if comments.pages > 1 %}
    {% set currentPage = comments.page %}
    <nav>
        <ul class="pagination">
            {% if currentPage > 1 %}
                <li class="page-item">
                    <a href="{{ path('trick_detail', {slug: trick.slug, page: currentPage - 1}) }}" class="page-link">&lt;</a>
                </li>
            {% else %}
                <li class="page-item disabled">
                    <a class="page-link">&lt;</a>    
                </li>
            {% endif %}
            {% for page in 1..comments.pages %}
                <li class="page-item {{ (page == currentPage) ? 'active' : '' }}">
                    <a href="{{ path('trick_detail', {slug: trick.slug, page: page}) }}" class="page-link">{{ page }}</a>
                </li>
            {% endfor %}
                {% if currentPage < comments.pages %}
                    <li class="page-item">
                        <a href="{{ path('trick_detail', {slug: trick.slug, page: currentPage + 1}) }}" class="page-link">&gt;</a>
                    </li>
                {% else %}
                    <li class="page-item disabled">
                        <a class="page-link">&gt;</a>    
                    </li>
                {% endif %}
            </li>
        </ul>
    </nav>
{% endif %}